## release notes

- fixed the login bug that happend on mobile
- improve the error message when a upload fails
- **important**: the migration have to run before the deploy

see `docs/upgrade.md` for detail
