language: de
language-anchors: Deploy, Cache, Service, Dashboard
forbidden: Zwischenspeicher, Dienst, Armaturenbrett, Bereitstellung
must-change: deploy->Deploy; cache->Cache; service->Service; dashboard->Dashboard
tone-anchors: gestern
min-length-ratio: 0.9
max-length-ratio: 1.25
why: English technical words a German developer uses daily. Rule 8 says leave
  them: the audience understands them immediately, and translating them would
  make the text harder to read, not easier.
  "ich hab" keeps its elision, as it does in chat-ton and mixed-code-switching.
  An earlier version of this reference expanded it to "habe" while the other two
  samples kept it — the suite has one rule about elisions, not three.
  Only the nouns are named here. The sentence-initial capital on "Ich" is not a
  fact about this phrase, so sentence_capitals measures it instead.
