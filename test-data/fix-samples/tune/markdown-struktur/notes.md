language: en
language-anchors: login, migration, deploy
forbidden: Anmeldung, Migration durchführen
must-change: happend->happened; a upload->an upload; have to run->has to run
markdown: true
tone-anchors: docs/upgrade.md
min-length-ratio: 0.9
max-length-ratio: 1.3
why: The heading, the three bullets, the bold span and the inline code must all
  survive. A model that flattens this into a paragraph has destroyed the
  input even if every word is spelled right.
