language: mixed
language-anchors: Branch, Pipeline, gemerged, flaky
forbidden: Zweig, Fließband
must-change: den branch->den Branch; die pipeline->die Pipeline
tone-anchors: I will look into it tomorrow morning, so ich hab
min-length-ratio: 0.9
max-length-ratio: 1.3
why: Rule 4 plus rule 8. The author switches to English for the last clause on
  purpose; translating it into German flattens exactly what rule 8 calls "how
  multilingual minds naturally reach for the nearest word".
  The elision "ich hab" is left alone deliberately — chat-ton keeps the same
  elision, and demanding it be expanded here while accepting it there would make
  the suite disagree with itself about what correct means. "rot" for a red
  pipeline is ordinary German developer speech, so it is not treated as a
  translation.
