language: de
language-anchors: Woche, Dateien
forbidden: week, files, size
must-change: fuer->Für; naechste->nächste; groesse->Größe; ausserdem->außerdem; waere->wäre; pruefung->Prüfung; koennten->könnten
tone-anchors: vorziehen
min-length-ratio: 0.85
max-length-ratio: 1.15
why: ASCII transliteration of umlauts, which is a spelling error rather than a
  style choice. Every one of them has a correct form.
